# App Store compliance checklist

> **Status: SKELETON — owned by AUDIT-003 (The App Review Rejector, session 004).**
> Session 001 laid out the table and pre-filled only rows it could verify by reading
> `project.yml`, the entitlements file, and `PrivacyInfo.xcprivacy` directly. Every other row
> is `UNKNOWN` on purpose. `UNKNOWN` is a legitimate status and must name what would resolve
> it — never upgrade a row to `PASS` without evidence pasted into the session log.

**AUDIT-003 must fetch the live App Store Review Guidelines before filling this in.** Do not
audit from memory; the guidelines change. Cite the live guideline text in every row you touch.

Status values: `PASS` · `FAIL` · `UNKNOWN` · `N/A`

---

## Build and target configuration

| # | Requirement | Status | Evidence | Fix / what would resolve |
|---|---|---|---|---|
| B1 | Bundle ID set and stable | PASS | `project.yml` → `com.rayancheca.prismrush` | — |
| B2 | Marketing version / build number | UNKNOWN | `project.yml` has `MARKETING_VERSION 1.0`, `CURRENT_PROJECT_VERSION 1` while the repo describes itself as v1.6 | Decide the shipping version; App Store Connect rejects a re-used build number |
| B3 | Minimum OS declared | PASS | `project.yml` → iOS 18.0 deployment target | — |
| B4 | Device family | PASS | `TARGETED_DEVICE_FAMILY: "1"` — iPhone only, so no iPad claim to honour | — |
| B5 | Orientation lock actually works | UNKNOWN | `INFOPLIST_KEY_UISupportedInterfaceOrientations: UIInterfaceOrientationPortrait` | Verify on device that nothing rotates or breaks |
| B6 | Launch screen present | UNKNOWN | `INFOPLIST_KEY_UILaunchScreen_Generation: YES` | Confirm the generated launch screen does not flash a blank frame before the splash |
| B7 | App icon present and correct | UNKNOWN | `PrismRush/Assets.xcassets/AppIcon.appiconset`, generated by `Tools/gen_icon.swift` | Verify 1024 icon has no alpha channel and no rounded corners baked in |
| B8 | Export compliance / encryption declaration | UNKNOWN | No `ITSAppUsesNonExemptEncryption` key found in `project.yml` | Add the key or answer the App Store Connect prompt each submission |

## Entitlements and capabilities

| # | Requirement | Status | Evidence | Fix / what would resolve |
|---|---|---|---|---|
| E1 | Game Center entitlement | UNKNOWN | `project.yml` declares `com.apple.developer.game-center: true` | Confirm the App ID in the developer portal has Game Center enabled and the leaderboards exist |
| E2 | Sign in with Apple entitlement | UNKNOWN | `com.apple.developer.applesignin: [Default]` | Confirm it is actually used; if it creates an account, C3 below becomes mandatory |
| E3 | iCloud KV store entitlement | UNKNOWN | `com.apple.developer.ubiquity-kvstore-identifier` set | Confirm the container exists and matches the App ID |

## Guideline 2.1 — completeness

| # | Requirement | Status | Evidence | Fix / what would resolve |
|---|---|---|---|---|
| C1 | No placeholder content, demo text, or non-functional features | UNKNOWN | — | AUDIT-001's Completeness Ledger feeds this row |
| C2 | No crash on launch on a clean install | UNKNOWN | — | Fresh-install run on a real device |
| C3 | Account deletion path, if an account is created | UNKNOWN | `AccountService.swift` exists; Sign in with Apple entitlement is declared | Trace whether an account is created; if so, an in-app deletion path is mandatory |

## Guideline 3.1 — in-app purchase

| # | Requirement | Status | Evidence | Fix / what would resolve |
|---|---|---|---|---|
| P1 | StoreKit 2 `VerificationResult` actually checked | UNKNOWN | `IAP/IAPManager.swift` | Read every purchase and restore path; `.unverified` must not be treated as `.verified` |
| P2 | Restore Purchases affordance present and working | UNKNOWN | — | Find it in the UI or file it as FAIL |
| P3 | No external payment path | UNKNOWN | — | Grep for any purchase link leaving the app |
| P4 | Randomized purchase odds disclosed | UNKNOWN | `UI/MysteryBoxView.swift` exists — a loot-box-shaped surface | If it is a paid randomized purchase, published odds are mandatory (3.1.1) AND required by the charter non-negotiable |
| P5 | Product IDs match between `Products.storekit` and the catalog | UNKNOWN | `Products.storekit`, `IAP/IAPCatalog.swift` | Diff them |

## Guideline 5.1 — privacy

| # | Requirement | Status | Evidence | Fix / what would resolve |
|---|---|---|---|---|
| V1 | `PrivacyInfo.xcprivacy` present | PASS | `PrismRush/Support/PrivacyInfo.xcprivacy` exists | — |
| V2 | Manifest accurately declares collected data | UNKNOWN | — | Compare the manifest against what `AccountService` / `GameCenterService` / `ProfileStore` actually collect |
| V3 | Required-reason API declarations complete | UNKNOWN | — | `UserDefaults` access is a required-reason API (`NSPrivacyAccessedAPICategoryUserDefaults`); confirm it is declared |
| V4 | Purpose strings for every permission requested | UNKNOWN | — | Enumerate requested permissions; there may be none |
| V5 | Privacy policy URL live and reachable | UNKNOWN | `Store/metadata.md` | The URL must resolve before submission |
| V6 | App Privacy answers match actual collection | UNKNOWN | — | Owner action in App Store Connect |
| V7 | Third-party SDK manifests + signatures | N/A | Repo declares zero third-party dependencies | Re-check if a dependency is ever added |

## Guideline 2.3 — metadata accuracy

| # | Requirement | Status | Evidence | Fix / what would resolve |
|---|---|---|---|---|
| M1 | Screenshots show the actual shipping app | UNKNOWN | `reports/shots/` holds per-version captures; `.gitignore` excludes `reports/shots/*.png` | Regenerate against the shipping build |
| M2 | Description promises only what ships | UNKNOWN | `Store/metadata.md` | Cross-check against the Completeness Ledger |
| M3 | Age rating matches content and mechanics | UNKNOWN | — | A randomized paid purchase affects this |

## Guideline 4.x — design and minimum functionality

| # | Requirement | Status | Evidence | Fix / what would resolve |
|---|---|---|---|---|
| D1 | Enough app to be a product | UNKNOWN | — | AUDIT-002 and AUDIT-004 feed this |
| D2 | No broken-looking states for normal situations | UNKNOWN | — | Owner decree 3 sets a higher bar than Apple does here |

---

## Owner actions that no agent session can complete

These can never move past `UNKNOWN` from inside a session. They belong to Rayan.

- App Store Connect: App Privacy questionnaire, age rating questionnaire, export-compliance answer
- Developer portal: Game Center leaderboard creation (`prismrush.best`, `prismrush.daily`), iCloud container
- A live, reachable privacy policy URL and support URL
- Real-device verification of anything marked `VERIFY-PENDING` in the backlog
