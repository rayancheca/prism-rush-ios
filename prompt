┌─────────────────────────────────────────────────────────────────────────────────────────────┐
│ SUPERSEDED — DO NOT FOLLOW. Historical artifact, 2026-06-12 (pre-session-001).              │
│                                                                                             │
│ This was a one-off session prompt for the CODE_REVIEW.md plan. That plan is long done. The  │
│ program is now governed by CLAUDE.md, docs/agent/01_RULES.md, docs/agent/02_STATE.md and    │
│ HANDOFF.md — read those instead.                                                            │
│                                                                                             │
│ It is bannered rather than deleted because line 10 below instructs an agent to enforce      │
│ "zero binary assets", a decree the OWNER REVOKED on 2026-08-03 (D-046). Following this file │
│ would undo an owner decision. Every other constraint it states has also been superseded at  │
│ least once. Kept only as a record of what session 000 was told.                             │
└─────────────────────────────────────────────────────────────────────────────────────────────┘

You are continuing work on Prism Rush (this repo). I just ran a full code review — it's in CODE_REVIEW.md at the repo root. Your job this session is to READ that review and IMPLEMENT its prioritized plan (§20). Work autonomously: make the best decision, document it in state.md, and keep going — don't stop to ask for confirmation, don't stop because something is hard.

BEFORE TOUCHING ANYTHING, read these three files in full and treat them as binding:
1. CODE_REVIEW.md   — the plan you're executing (§18 issues, §20 ordered recommendations)
2. CLAUDE.md        — the owner decrees (product law) and iron rules (load-bearing invariants)
3. state.md         — current status; you will update it as you go

NON-NEGOTIABLE CONSTRAINTS (violating any of these is a failed session):
- The deterministic core is sacred. Core/ randomness flows ONLY through the seeded SplitMix64. The worlds work below is PURELY VISUAL — it may touch Render/, UI/Theme.swift, UI/WorldPreviewCanvas.swift, and Audio music layering ONLY. It must NOT touch Core/, must NOT consume any spawn RNG, and must NOT change DailyChallenge.layoutVersion (a visual overhaul does not change the sim, so the solvability bot stays green and every seed stays stable). If you ever find yourself editing Core/ for the worlds, stop — you've taken a wrong turn.
- Owner decrees still hold: a character NEVER follows the world palette (don't reintroduce world-driven skin colors); previews never lie (if the in-game world look changes, WorldPreviewCanvas must change to match); zero binary assets (meshes via MeshDescriptor, audio via Synth DSP, the only catalog entry is the generated AppIcon); no broken-looking states; clarity beats spectacle (calm UI).
- Keep the G3 @Observable rules (reference singletons live in body, never snapshot store.profile into a top-of-body let).
- Verify continuously: after any Core/Meta/Synth-adjacent change run `swift test -c release` (the ~89-test Linux suite incl. the solvability bot) and keep it green. For Render/UI/audio/StoreKit changes, build on the Mac: `./Tools/build.sh`, and `./Tools/ci.sh` for the full gate. Do NOT run screenshots/simctl on the dev sim while xcodebuild test is running on it.
- Work one step at a time. Run it, see it pass, then commit (conventional commits, match the repo's existing message style — e.g. `feat(v1.4.3): ...`, `fix:`, `docs:`). Update state.md after each step. Never combine steps, never leave the suite red.

THE PLAN — implement in this order (from CODE_REVIEW.md §20):

1. CHEAP, HIGH-CREDIBILITY DOC/CLAIM FIXES (do these first — fast wins, low risk):
   - README.md: correct the test split to "147 unit + 11 XCUITest = 158" (verify by counting `func test` in Tests/CoreTests + UITests). Fix the IAP count to 7 everywhere it says 5 (the catalog and Products.storekit have 7). Qualify "zero binary assets" (the lone AppIcon PNG is generated by Tools/gen_icon.swift — say "zero hand-authored binary assets"). Soften "alloc-free steady state" to "material-allocation-free" (per §4/§13 — material instances are cached, but per-frame `materials = [mat]` literals and one CoW snapshot copy remain).
   - Tools/ci.sh: delete the stale comment block (lines ~11-16) claiming "no tests until Phase 2" — 147 tests exist now.
   - reports/AGENT_docs.md and reports/AGENT_wiring.md are stale and contradict shipped reality (privacy "Data Not Collected" is wrong; the prismrush.daily board now exists). Add a one-line "SUPERSEDED — see Store/metadata.md / SHIP_CHECKLIST.md" banner at the top of each rather than deleting history.

2. THE WORLDS — the owner's top concern (CODE_REVIEW.md M1 + §20.1, recommended option: distance-driven EVOLUTION).
   Today world identity is keyed `world % 3` in every layer, so worlds 6/9/12 are the same three families reshuffled, and WorldSky's cycle-richening is clamped off (`min(cycle, 2)`). Make the worlds visibly INTENSIFY with the absolute ordinal instead of repeating:
   - Drive palette, decor density, sky element counts, tint/atmosphere, and music layering as continuous functions of the ABSOLUTE worldOrdinal (already plumbed in GameSnapshot.worldOrdinal and fed to WorldDecor/WorldSky), not `ordinal % 3`.
   - Remove the `min(cycle, 2)` clamps in WorldSky so cycle 3+ keeps differentiating; add per-cycle hue rotation and/or added set-piece layers so deeper runs feel richer and distinct, not looped.
   - Keep the 3 archetype families as the base palette, but make each successive cycle a clear transformation (hue shift, density, fog, added layers). The goal: a player at world 7 should not feel "Neon Metropolis, again."
   - Mirror every visible change into UI/WorldPreviewCanvas.swift so the previews stay honest (decree 2).
   - Confirm: no Core/ edits, no RNG-path changes, layoutVersion unchanged, `swift test -c release` still green, and a Mac build is clean. Capture a deep-world screenshot (PR_WORLD=7) to confirm the evolution reads, and save it under reports/shots/.
   - If you judge the full procedural-recipe approach (§20.1 option 2) is cleaner, you may do that instead — but only if it stays purely visual and sim-safe. Document the choice in state.md.

3. CI: add a macOS GitHub Actions job (CODE_REVIEW.md M5/§20.3) that builds the app and runs the unit bundle on macos-latest (the current workflow is Linux-only and never compiles the renderer/audio/SwiftUI).

4. CHEAP TEST GAPS (CODE_REVIEW.md M4/§20.4): add unit tests for IAPManager's pure value-badge math (coinsPerUnit / badge selection) and availability-state transitions, plus the pure view-logic helpers that are deterministic (e.g. heroOffer / nextUnlockSkin / RewardsBar priority ladder). These are pure — no StoreKit/GameKit needed.

5. DYNAMIC TYPE (CODE_REVIEW.md M3/§20.5): migrate ProfileView.swift, HowToPlayView.swift, and PauseOverlay.swift off hardcoded `.system(size:)` to the existing Theme.TypeScale `.typeScale(...)` / @ScaledMetric system so the tutorial and pause menu scale with text size.

6. SOLVABILITY SOAK (CODE_REVIEW.md §20.6): raise the deep-soak seed count in SolvabilityBotTests (the 12,000 m run is only 10 seeds) — it's cheap. Keep it green.

7. SMALLER ITEMS: make ShopView.heroShell generic to drop the AnyView wraps (M6); resolve the revive() inconsistency (it clears boost/flow but not magnet/doubler/chrono — pick one and make the comment honest, L1); move the Apple user id from UserDefaults to Keychain in AccountService (L3).

8. If you can run a Mac simulator: re-capture the README walkthrough screenshots against current main so they reflect the new worlds + the 24-character roster (docs/screenshots/). If you can't, note it in state.md as an outstanding human step.

When you finish (or hit a genuine blocker): make sure `swift test -c release` is green, the Mac build is clean, state.md reflects exactly what changed and what's left, and everything is committed. Then summarize what you did, what you verified, and what still needs a human (the App Store Connect gates in SHIP_CHECKLIST.md are human-only — don't attempt them).

Start by reading CODE_REVIEW.md, CLAUDE.md, and state.md, then begin step 1.
